/** Stable host id for the injected sidebar. */
export const SIDEBAR_HOST_ID = 'vendelo-wa-root'

/** Width reserved for the sidebar when open (px). */
export const SIDEBAR_WIDTH_PX = 340

/** localStorage key for open/collapsed preference. */
export const SIDEBAR_OPEN_STORAGE_KEY = 'vendelo360:sidebar-open:v1'
