# Cloudflare Pages — landing (@broker/landing)
#
# Project settings (dashboard):
#   Root directory: /
#   Build command:  pnpm install && pnpm --filter @broker/landing build
#   Output directory: apps/landing/dist
#   Custom domains: vendeya.app (apex) + www → 301 to apex
#
# Build env:
#   PUBLIC_SITE_URL=https://vendeya.app
#
# See docs/deploy_railway.md
