import { Warehouse } from 'lucide-react'
import type { AuthPortalBranding } from '@broker/ui'

export const portalTheme: AuthPortalBranding = {
  variant: 'provider',
  badge: 'Portal proveedores',
  tagline: 'Gestiona tu catálogo, pedidos y operaciones B2B',
  icon: Warehouse,
}
