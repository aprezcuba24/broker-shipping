# Cloudflare Pages — portal gestores (@broker/seller)
#
# Project settings (dashboard):
#   Root directory: /
#   Build command:  pnpm install && pnpm --filter @broker/seller build
#   Output directory: apps/seller/dist
#   Custom domain: gestores.vendeya.app
#
# Build env:
#   VITE_API_URL=https://api.vendeya.app
#
# See docs/deploy_railway.md
