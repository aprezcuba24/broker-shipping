/**
 * Broker dialog shell classes.
 * Positioning uses Tailwind utilities so `twMerge` can replace DialogContent defaults
 * (`left-[50%]`, `translate-x-[-50%]`, etc.). A bare `broker-dialog` CSS class cannot.
 */
export const BROKER_DIALOG_CLASSNAME =
  'broker-dialog left-4 right-4 top-4 w-auto max-w-none translate-x-0 translate-y-0 p-4 gap-3 rounded-xl sm:left-1/2 sm:right-auto sm:top-1/2 sm:w-full sm:max-w-md sm:-translate-x-1/2 sm:-translate-y-1/2 sm:p-5'
